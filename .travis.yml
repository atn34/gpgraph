language: cpp
compiler:
    - g++-5

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test

before_install:
    - sudo ./install-deps.sh

script:
    - ./build.sh
    - ./test.sh

after_success: 
    - bash <(curl -s https://codecov.io/bash)
